#include "main.h"

/**
 * jack_bauer - checks for lowercse
 *
 * @c: the input
 *
 * Return: 1 if lowercase or 0 if upper
 */

void jack_bauer(void)
{
	for (int i = 0; i <= 23; i++)
	{
		_putchar(i);
		_putchar(':');
		for(int j = 0; j <= 59; j++)
		{
			_putchar(j);
		}
	}
